<script>
    <% if user_signed_in? %>
        window.current_user = <%= current_user.to_json(:methods => []).html_safe %>;
        window.default_destination = <%= Place.where(:city => "San Francisco") %>;
    <% end %>
</script>